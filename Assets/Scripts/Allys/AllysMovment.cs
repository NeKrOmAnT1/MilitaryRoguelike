using System.Collections.Generic;
using UnityEngine;

public class AllysMovment : MonoBehaviour
{    
    

    // Start is called before the first frame update
    private void Awake()
    {
        
    }

    // Update is called once per frame
    private void FixedUpdate()
    {
        
    }

    private void AllysMove()
    {
        
    }
}
